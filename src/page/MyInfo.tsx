import React from 'react';
import AppLayout from '../common/AppLayout';

const MyInfo = () => {
    return (
        <AppLayout>
            MyInfo
        </AppLayout>
    );
};

export default MyInfo;